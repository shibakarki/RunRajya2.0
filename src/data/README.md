Drop the `rupandehi_boundary.json` file from the previous project here
(2,653 coordinate points mapping Rupandehi's OSM border). Used by
DistrictMapPreview on Home and as the reference boundary for the
4,814-cell grid seeded into the `zones` table.
